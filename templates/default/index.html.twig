{% extends 'base.html.twig' %}

{% block title %}Title{% endblock %}

{% block body %}
    {# form.parent is equal to the child parent (this is the bug) #}
    {{ dump(form.parent == form.children.parent) }}

    {# With our custom twig extension the correct parent is null and not the child #}
    {{ dump(get_form_view_parent(form) == form.children.parent) }}
{% endblock %}
